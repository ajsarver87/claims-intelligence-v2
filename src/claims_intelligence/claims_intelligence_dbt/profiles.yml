claims_intelligence_dbt:
  outputs:
    dev:
      dataset: dbt_dev
      job_execution_timeout_seconds: 300
      job_retries: 1
      keyfile: "{{ env_var('GOOGLE_APPLICATION_CREDENTIALS') }}"
      location: us-central1
      method: service-account
      priority: interactive
      project: claims-intelligence
      threads: 1
      type: bigquery
  target: dev
